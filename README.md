

1.ans
**from a div (by id) get a single element by this getElementById method
**from a div (dy id) get a single element by this getElementById method
** from html full code like index.html this file from if i call querySelector method by id name its get first id name from this full file
**querySelector OR querySelectorAll just difference its that querySelector get only one id or querySelectorAll get all same name id

2.ans
create a element the dom create new element this type cosnst Div = document.createElement('div');

Div.innerText = 'Hello!';

get body (document.body).appendChild(Div); this methods create and insert

3.ans
Event Bubbling is a dom event concept where when an event create on an HTML element (like click or keypress), the event first happens on the target element and then bubbles up to its parent elements, all the way up to the document and window.

4.ans
Event Delegation is a pattern that its handle multiple event and after its add a parent function after tis control by target event.value this type

5.ans
event.preventDefault()   is a methods that browser not load function working withoud browser load 
event.stopPropagation() Stops the event to parent elements,  it only triggers on the target element.

like  Clicking a button inside a div without triggering the div’s click event.
